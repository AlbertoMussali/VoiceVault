# Ralph config (may contain API key)
.cursor/ralph-config.json
.cursor/ralphex-config.json

# Environment files
.env
.env.local
.env.*.local

# Ralphex runtime artifacts
.ralph-worktrees/
.ralph/parallel/
.ralph/session_id

# Local ralphex logs/state (keep out of git to avoid merge churn)
.ralph/
